<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools">

  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- I dont remember, probs has to do with downloads -->
  <uses-permission android:name="android.permission.INTERNET" /> <!-- unless you only use cs3 as a player for downloaded stuff, you need this -->

  <!-- Storage permissions for different Android versions -->
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
    android:maxSdkVersion="32" /> <!-- Android 6-12 -->
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
    android:maxSdkVersion="29" /> <!-- Android 6-9, not needed on 10+ -->

<!--  &lt;!&ndash; Android 13+ (API 33+) media permissions &ndash;&gt;-->
  <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" /> <!-- Videos -->
  <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" /> <!-- Music/Audio -->
<!--  <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" /> &lt;!&ndash; Pictures &ndash;&gt;-->

<!--  &lt;!&ndash; All Files Access for Android 11+ (API 30+) &ndash;&gt;-->
<!--  <uses-permission-->
<!--    android:name="android.permission.MANAGE_EXTERNAL_STORAGE"-->
<!--    tools:ignore="ScopedStorage" /> &lt;!&ndash; Full storage access &ndash;&gt;-->
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" /> <!-- some dependency needs this -->
  <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" /> <!-- Used for player vertical slide -->
  <uses-permission android:name="android.permission.WAKE_LOCK" /> <!-- Used for torrent downloads -->
  <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" /> <!-- Used for background downloads -->
<!--  <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" /> &lt;!&ndash; Used for app update &ndash;&gt;-->
  <!-- POST_NOTIFICATIONS permission is NOT declared here to avoid showing permission request at install time -->
  <!-- It will be requested on-demand when user initiates a download (Android 13+) -->
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" /> <!-- Used for update service -->
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" /> <!-- Used for torrent service -->
  <!-- Removed FOREGROUND_SERVICE_MEDIA_PLAYBACK - app does not need background video playback -->
  <!-- Required for getting arbitrary Aniyomi packages  -->
<!--  <uses-permission-->
<!--    android:name="android.permission.QUERY_ALL_PACKAGES"-->
<!--    tools:ignore="QueryAllPackagesPermission" />-->

  <!-- Fixes android tv fuckery -->
  <uses-feature
    android:name="android.hardware.touchscreen"
    android:required="false" />
  <uses-feature
    android:name="android.software.leanback"
    android:required="false" />

  <uses-feature android:glEsVersion="0x00020000" />

  <queries>
    <package android:name="org.videolan.vlc" />
    <package android:name="com.instantbits.cast.webvideo" />
    <package android:name="cloud.streamless.torream" />
  </queries>

  <application
    android:name=".CSApplication"
    android:allowBackup="true"
    android:appCategory="video"
    android:banner="@mipmap/ic_banner"
    android:dataExtractionRules="@xml/data_extraction_rules"
    android:fullBackupContent="@xml/backup_rules"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme"
    android:usesCleartextTraffic="true"
    tools:targetApi="31">

    <!-- Declare 16KB page size support -->
    <property
      android:name="android.app.16kb_page_size.enabled"
      android:value="true" />

    <service
      android:foregroundServiceType="dataSync"
      android:name=".utils.PackageInstallerService"
      android:exported="false"
      tools:ignore="ForegroundServicePermission" />

    <service
      android:name=".download.DownloadService"
      android:foregroundServiceType="dataSync"
      android:exported="false"
      tools:ignore="ForegroundServicePermission" />

    <!-- WorkManager's foreground service for running workers -->
    <service
      android:name="androidx.work.impl.foreground.SystemForegroundService"
      android:foregroundServiceType="dataSync"
      android:exported="false"
      tools:node="merge" />

    <!-- Disable default WorkManager initialization - using custom configuration -->
    <provider
      android:name="androidx.startup.InitializationProvider"
      android:authorities="${applicationId}.androidx-startup"
      android:exported="false"
      tools:node="merge">
      <meta-data
        android:name="androidx.work.WorkManagerInitializer"
        android:value="androidx.startup"
        tools:node="remove" />
    </provider>

    <meta-data android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
      android:value="cloud.streamless.torream.utils.CastOptionsProvider"/>

    <activity
      android:name=".MainActivity"
      android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboard|keyboardHidden|navigation"
      android:exported="true"
      android:launchMode="standard"
      android:resizeableActivity="true"
      android:supportsPictureInPicture="true">


      <!-- Launcher intent -->
      <intent-filter android:exported="true">
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
        <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
      </intent-filter>

      <!-- Custom scheme for deep linking -->
      <intent-filter android:priority="100">
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="csplayer" />
      </intent-filter>

      <!-- Video playback from all sources (merged file/content/http/https) -->
      <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="file" />
        <data android:scheme="content" />
        <data android:scheme="http" />
        <data android:scheme="https" />
        <data android:mimeType="video/*" />
        <data android:mimeType="audio/*" />
      </intent-filter>

      <!-- Playlist/JSON data from other apps (e.g., CinemaHD) -->
      <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="content" />
        <data android:mimeType="application/json" />
      </intent-filter>

      <!-- Custom download URL action from other apps -->
      <intent-filter android:exported="true" android:priority="100">
        <action android:name="cloud.streamless.torream.action.DOWNLOAD_URL" />
        <action android:name="cloud.streamless.torream.debug.action.DOWNLOAD_URL" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
    </activity>

    <activity
      android:name=".ControllerActivity"
      android:exported="false" />

    <!-- Share Receiver Activity - Dialog style for receiving URLs from other apps via ACTION_SEND -->
    <activity
      android:name=".ui.dialog.ShareReceiverActivity"
      android:theme="@style/ShareReceiverDialogTheme"
      android:exported="true"
      android:launchMode="singleInstance"
      android:excludeFromRecents="true"
      android:taskAffinity=""
      android:noHistory="true">

      <!-- Handle ACTION_SEND for text/plain (URLs shared from browsers, etc.) -->
      <intent-filter android:priority="100">
        <action android:name="android.intent.action.SEND" />
        <category android:name="android.intent.category.DEFAULT" />
        <data android:mimeType="text/plain" />
      </intent-filter>

      <!-- Handle magnet links and torrents -->
      <intent-filter android:priority="100">
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="magnet" />
      </intent-filter>
    </activity>

    <provider
      android:name="androidx.core.content.FileProvider"
      android:authorities="${applicationId}.provider"
      android:enabled="true"
      android:exported="false"
      android:grantUriPermissions="true">
      <meta-data
        android:name="android.support.FILE_PROVIDER_PATHS"
        android:resource="@xml/provider_paths" />
    </provider>

    <!-- Removed BackgroundPlaybackService - app does not need background playback -->
    <!-- Removed NotificationButtonReceiver - no longer needed without background service -->

    <!-- AdMob App ID (Replace with your actual AdMob App ID) -->
    <meta-data
      android:name="com.google.android.gms.ads.APPLICATION_ID"
      android:value="ca-app-pub-3940256099942544~3347511713"/>

    <!-- Fix for Firebase Analytics and AdMob conflict -->
    <property
      android:name="android.adservices.AD_SERVICES_CONFIG"
      android:resource="@xml/gma_ad_services_config"
      tools:replace="android:resource" />

    <!-- Download notification receiver for Pause All and Cancel All actions -->
    <receiver
      android:name=".download.worker.DownloadNotificationReceiver"
      android:exported="false">
      <intent-filter>
        <action android:name="cloud.streamless.torream.PAUSE_ALL_DOWNLOADS" />
        <action android:name="cloud.streamless.torream.CANCEL_ALL_DOWNLOADS" />
      </intent-filter>
    </receiver>

    <!-- Continue watching notification action receiver -->
    <receiver
      android:name=".utils.ContinueWatchingActionReceiver"
      android:exported="false">
      <intent-filter>
        <action android:name="cloud.streamless.torream.CW_SNOOZE_1H" />
        <action android:name="cloud.streamless.torream.CW_SNOOZE_2H" />
        <action android:name="cloud.streamless.torream.CW_DISMISS" />
      </intent-filter>
    </receiver>
  </application>

</manifest>
